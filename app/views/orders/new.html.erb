<div class="row">
    <div class="col-lg-12">
        <h2 class="page-header"></h2>
    </div>
    <!-- /.col-lg-12 -->
</div>

<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
                <div class="panel-heading">
                  New Order
                </div>
                <!-- Tab panes -->
                <div class="panel-body">
                    <div class="tab-pane fade in active" id="home-pills">
                    	<%= form_for(@order) do |f| %>
                        <% if @order.errors.any? %>
                          <div class="alert alert-danger">
                            <h2><%= pluralize(@order.errors.count, "error") %> prohibited this user_restaurant from being saved:</h2>

                            <ul>
                              <% @order.errors.full_messages.each do |message| %>
                                  <li><%= message %></li>
                              <% end %>
                            </ul>
                          </div>
                        <% end %>
                        <form role="form" method="post">
                            <fieldset>
                                <div class="form-group">
                                    <label>Food Name</label>
                                    <%= @food.name %>
                                    <input value="<%= @food.id %>" type="hidden" name="food_id" id="order_food_id" />
                                </div>
                                <div class="form-group">
                                    <label>Restaurant</label>
                                    <%= @restaurant.name %>
                                </div>
                                <div class="form-group">
                                    <label>Price</label>
                                    <%= @food.price %>
                                </div>
                                <div class="form-group">
                                    <%= f.label :address %>
                                    <%= f.text_field :address, class: 'form-control', :value => @customer.address %>
                                </div>
                                <div class="form-group">
                                    <%= f.label :zip %>
                                    <%= f.text_field :zip, class: 'form-control', :value => @customer.zip %>
                                </div>
                                <div class="form-group">
                                    <%= f.label :phone %>
                                    <%= f.text_field :phone, class: 'form-control', :value => @customer.phone %>
                                </div>

                                <button type="submit" class="btn btn-info">Order</a>
                            </fieldset>
                        </form>
                        <% end %>
                    </div>
                </div>
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-6 -->
</div>
<!-- /.row -->