<div class="row">
    <div class="col-lg-12">
        <h2 class="page-header"></h2>
    </div>
    <!-- /.col-lg-12 -->
</div>

<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <%= form_for(@order) do |f| %>
            <form role="form" method="post">
                <div class="panel-heading">
                  New Order
                </div>
                <div class="panel-body">
                    <div class="table-responsive">
                        <div class="form-group">
                            <label>Restaurant</label>
                            <%= @restaurant.name %>
                        </div>
                        <table class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Food Name</th>
                                    <th>Price</th>
                                    <th>Quantity</th>
                                </tr>
                            </thead>
                        <% @cart.each do |item| %>
                            <tbody>
                                <tr>
                                    <td><%= item["food_id"] %></td>
                                    <td><%= item["food"].name %></td>
                                    <td><%= item["food"].price %></td>
                                    <td>
                                        <input class="form-control" value="<%= item["count"] %>" size="3" type="text" name="food_count_<%= item["food_id"] %>" />
                                    </td>
                                </tr>
                            </tbody>
                        <% end %>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->

                <!-- Tab panes -->
                <div class="panel-body">
                    <div class="tab-pane fade in active" id="home-pills">
                        <% if @order.errors.any? %>
                          <div class="alert alert-danger">
                            <h2><%= pluralize(@order.errors.count, "error") %> prohibited this user_restaurant from being saved:</h2>

                            <ul>
                              <% @order.errors.full_messages.each do |message| %>
                                  <li><%= message %></li>
                              <% end %>
                            </ul>
                          </div>
                        <% end %>
                            <fieldset>
                                <div class="form-group">
                                    <%= f.label :address %>
                                    <%= f.text_field :address, class: 'form-control', :value => @customer.address %>
                                </div>
                                <div class="form-group">
                                    <%= f.label :zip %>
                                    <%= f.text_field :zip, class: 'form-control', :value => @customer.zip %>
                                </div>
                                <div class="form-group">
                                    <%= f.label :phone %>
                                    <%= f.text_field :phone, class: 'form-control', :value => @customer.phone %>
                                </div>

                                <button type="submit" class="btn btn-info">Order</a>
                            </fieldset>
                    </div>
                </div>
            </div>
            <!-- /.panel-body -->
            </form>
            <% end %>
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-6 -->
</div>
<!-- /.row -->